Angular ES6 JSPM Gulp Boilerplate
=====================================

1. Run `npm install` to install the npm modules
2. Run `jspm install` to install the jspm modules
3. Run `gulp dev` (may require installing Gulp globally `npm install gulp -g`)